// COLORS
const whiteColor = '#ffffff';

const MarvelSafeAreaView = {
    flexDirection:'column',
    flex: 1,
    width: '100%'
};

const MarvelScrollView = {
    flexGrow: 1, 
    justifyContent: 'center',
    backgroundColor: whiteColor
};

export { 
    whiteColor,
    MarvelSafeAreaView, 
    MarvelScrollView
}