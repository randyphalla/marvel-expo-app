import React from 'react';
import { SafeAreaView, ScrollView, Text } from 'react-native';

export default function Comics({navigation, route}: any) {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>Comics</Text>
      </ScrollView>
    </SafeAreaView>
  )
}